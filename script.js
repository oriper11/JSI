function agregarNotas(){ 
    let nombre = prompt ("Ingrese el nombre completo del estudiante")
    console.log(nombre)
    let nota1 = parseInt(prompt ("Ingrese primera nota"))
    let nota2 = parseInt(prompt ("Ingrese segunda nota"))
    let nota3 = parseInt(prompt ("Ingrese tercer nota"))
    
    if (nota1 == ""){
        alert ("Los espacios no pueden quedar vacios")
    }
    if (nota2 == ""){
        alert ("Los espacios no pueden quedar vacios")
    }
    if (nota3 == ""){
        alert ("Los espacios no pueden quedar vacios")
    }
    function operacion() {
        let nota1=
         

    }
}